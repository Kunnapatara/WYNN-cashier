## Packages
framer-motion | Page transitions and smooth micro-interactions for a premium feel
lucide-react | Clean, minimal icons

## Notes
The application needs to interact with `GET /api/cashier/resolve` passing `giftId` and `country` as query parameters.
Using TanStack Query with conditionally enabled queries based on form submission.
